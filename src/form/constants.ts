export enum REORDER_DIRECTION {
  UPWARDS = "UPWARDS",
  DOWNWARDS = "DOWNWARDS",
}

export enum QUESTION_TYPE {
  NUMERIC = "numeric",
  RANGE = "range",
  SELECT = "select",
  RADIO = "radio",
  TEXT = "text",
  CHECK = "check",
}
